Use the Explore subagent (very thorough) to survey this CAP project and report:
  1. Every CDS entity defined in db/ — name, mixins, key associations
  2. Every service in srv/ — path, auth, exposed entities, custom actions/functions
  3. Every UI app in app/ — type (freestyle vs Fiori Elements), views, routes
  4. All custom handlers (srv/handlers/*) — what entity/event they hook
  5. Auth implementation — JWT helpers, middleware, password hashing, init scripts
  6. Any .http test files and what endpoints they cover