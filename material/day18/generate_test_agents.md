create 2 sub agents for our sap fiori app of freestyle type

use this sub agent to create opa5 tests in free style fiori app features, that test end to end navigation and load the next page to cross check if the data is pouplated on next screen. also make sure that the data exatctly match in case the test case dealing with navigation from table row or a popup opening to show details. the tests must be created in the test folder under webapp and an indexTestFile.html with feature name appended to execute the test, make sure the code is clean without any reference errors. most importantly the test should also need to login with credentials to arrive to the main screen for traveller, so use dummy user raj.sharma@example.com and password as Welcome1! the test will also fail if the navigation after login does not work. all these steps must be part of test so later when execution starts for the tests, system should automatically take care of login and navigation to the screen which we want to test.

use this sub agent to run the opa5 test created for free style fiori app features in a headless browser, the agent should by default run in headless browser however should ask developer to run test in foreground and background. once the test execution is completed, a summary to be presented to the developer regarding the journeys tested and results of testcases.